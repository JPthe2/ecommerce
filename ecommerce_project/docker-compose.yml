services:
  web:
    build: .
    container_name: django-app
    restart: unless-stopped
    env_file:
      - .env
    ports:
      - "8020:8020"
    command: gunicorn ecommerce.wsgi:application --bind 0.0.0.0:8020
